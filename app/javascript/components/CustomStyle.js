import { ThemeProvider, createTheme } from "@mui/material/styles";


export const theme = createTheme({
    palette: {
      primary: {
        main: "#d32f2f"
      },
      secondary: {
        main: "#0288d1"
      },
      mode: "dark"
    }
  });

