<div style=" ">
      <% flash.each do |key, value| %>
            <div class="<%= flash_class(key) %> rounded-0 border-0" style="margin-bottom: 0px">
                  <span class="text-white"><%= value %></span>
            </div>
      <% end %>
</div>
